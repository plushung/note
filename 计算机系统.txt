
线程与进程调度算法有哪些？
	先来先服务（队列）	
	最短优先调度算法（优先队列）：优先调度短作业或短进程
	高优先权优先调度算法：
	基于时间片的轮转调度算法：（1）时间片轮转法：为进程分配cpu的时间片，执行完后中断cpu。
	

将对称下三角矩阵的元素存到一维数组，矩阵各元素在一维数据组中的对应关系？
	矩阵为i行j列,以行为主的次序, 在一维数组中的位置
	Aij=j(j-1)/2+i (i>=j 下三角的元素)--1
	Aij=i(i-1)/2+j (i<=j 上三角的元素)--2
	,以列为主的次序则1与2调转