表现层
	采用MVC的模式，将Model渲染到View上去，由Controller来控制流程
业务层
	每个方法都是某一业务的实现，每一个业务都包含多个对数据库的操作，调用多个不同Dao层的方法来完成某一业务。
	并要保证业务层方法中的每个Dao操作都要同时完成要么全都rollback。TransactionManager就是对业务层的方法进行事务管理的。
持久层
	Dao层，一个Dao就是对一个表的数据访问。有多个Dao，来满足业务层的需要。